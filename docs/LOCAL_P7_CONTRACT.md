# Local P7 contract precedence

The root README and the P7 phase change register govern version 0.1.0.dev1.
Other documents inherited from the original import describe historical snapshots.
The old requirements-lock.txt records a past environment; ENVIRONMENT_OBSERVED_P7.json
records this qualification environment. Neither is a portable, complete lockfile.

Certified stored-input weighted-cut routines: forest, supplied-decomposition
treewidth, exhaustive and branch-and-bound with no numerical callback. Exact
rational records are primary; floating display endpoints are outward rounded.

Unenclosed callbacks, scenario aggregation and floating-point dynamics retain
explicit numerical-only status. The empty-queue condition alone does not establish
numerical exactness. The core DS1 result is a finite-domain qualification.

Author-only licence, contact, journal, AI disclosure and remote-action decisions
remain separate. No CI badge is earned by a workflow file merely being present.
