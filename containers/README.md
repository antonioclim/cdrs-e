# Container candidate

The Dockerfile is a build specification only. Docker/Podman was not available
in the Phase XLII runtime, so container execution is an explicit residual gate.
The package itself was tested through two isolated virtual-environment installs
from the wheel and source distribution.
