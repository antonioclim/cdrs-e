# 0.1.0rc1 — P10-SD40 bounded public-identity correction and full local re-freeze

- Change only the first-line module docstring in `src/cdrse/__init__.py` from the private-candidate wording to `CDRS-E auditable research software release candidate.`
- Preserve every subsequent byte in that file, preserve the other 28 registered files byte-identically and prove AST identity after removing the module docstring.
- Retain the publication-durable repository identity and software-only Apache-2.0 surface prepared in P10-SD39.
- Reconstruct the source ZIP, wheel and sdist twice, require byte identity, verify distribution metadata and rerun the complete source and installed-wheel suites.

# 0.1.0rc1 — P10-SD39 local publication-durability draft (not promoted)

- Bind the canonical repository identity `https://github.com/antonioclim/cdrs-e` in CodeMeta, PEP 621 metadata and citation metadata.
- Remove private GitHub/Zenodo operation plans from the public source tree and retain them only in the private transition wrapper.
- Replace volatile authorisation and non-existence assertions with durable artefact state.
- Complete 1,280/1,280 source tests, then stop fail-closed before distribution construction because the registered package module docstring still described the candidate as private.

# 0.1.0rc1 — P10-SD37 software-only local re-freeze

- Exclude `paper/`, `evidence/`, `provenance/` and `reports/` from every proposed public software surface while preserving excluded bytes in a private local audit wrapper.
- Preserve the 29-file scientific payload byte-identically.
- Correct release gates to QUICK `PASS` and FULL `PASS_WITH_ORCHESTRATION_QUALIFICATION`, retaining the interrupted parent attempt and 28/28 reconciled independent shards.
- Reconstruct deterministic source ZIP, wheel and sdist and rerun source and installed-wheel tests.
- At the SD37 checkpoint, repository, tag, release, archival and submission operations had not been executed; that state is retained as historical provenance.

# 0.1.0rc1 — local P10-SD32 release candidate

- Apply Apache License 2.0 locally through `LICENSE`, `NOTICE` and package metadata.
- Promote the software identity from `0.1.0.dev7` to `0.1.0rc1` without modifying the scientific algorithms.
- Update citation, CodeMeta, release-gate and build-recipe metadata for the prepare-only GitHub + Zenodo route.
- Add the P10-SD32 licence/promotion regression lock and rerun source and installed suites.
- Keep all remote publication, release, deposition, submission and cost authority closed.

# 0.1.0.dev7 — local P10-SD12 qualification candidate

- Bind and locally hash-verify the exact upstream `setuptools 84.0.0` and `pip 26.2.1` wheel bytes used by the build.
- Build and qualify the dev7 wheel and sdist under the fixed epoch `1789430400`.
- Repair F41 by deterministically normalising the sdist TAR/GZIP envelope after backend construction while preserving every member payload.
- Qualify a new immutable dev7 runtime and the declared QUICK/FULL replay without changing the scientific algorithms inherited from the corrected SD10 source candidate.
- Preserve the absence of independent-machine reproduction, public-release authority, licence selection and submission authority.

# 0.1.0.dev7 — corrected unpromoted P10-SD10 source candidate

- Repair the internal contradictions in the supplied SD9 source candidate and preserve the observed baseline failures.
- Bind curated official PyPI release-file metadata for `setuptools==84.0.0` and `pip==26.2.1`, including SHA-256 values, while explicitly recording that the corresponding bytes were not acquired locally.
- Record verified signed Git tag identities for the two toolchain releases and retain commit-SHA pins for GitHub Actions.
- Align the source recipe, hash lock, build script, version metadata and release gates without building or qualifying a dev7 wheel, sdist or runtime.
- Preserve `0.1.0.dev6` as the last qualified software identity and retain all publication, licence and remote-action gates as closed.

# 0.1.0.dev6 — local P10-SD8 build-closure promotion candidate

- Promote F35 by pinning the build backend exactly to `setuptools==82.0.1` and enforcing `SOURCE_DATE_EPOCH=1789430400`.
- Add machine-readable build recipe and hash-bound local backend lock records.
- Add the six-case SD8 pre-patch regression lock, yielding 1,209 finite correlated software obligations.
- Retain all qualified dev5 algorithmic, numeric and source-boundary behaviour unchanged.
- Preserve explicit limits: the captured backend wheel is not upstream-authenticated and no independent-machine, multi-platform or public-release claim is made.

# 0.1.0.dev5 — local P10-SD6 promotion candidate

- Promote the hash-bound SD5 hardening of exact-rational subset validation, exact witness-domain checks, endpoint tolerance validation, strict direct-Python numerical boundaries and canonical mapping-key hashing (F29–F32).
- Include the byte-preserved eight-case P3 file in the ordinary source/test tree, making K2 and the source archive autonomous for the 1,180 historical obligations (F33).
- Make the three finite audit utilities operate on pristine trees while stating their bounded authority (F34).
- Add the unchanged 23-case SD5 pre-patch lock, yielding 1,203 finite correlated software obligations.
- Retain the qualified compositional and economic-boundary repairs from dev3 and dev4. No public release, licence, DOI, deposit, remote repository write or submission is authorised.

# 0.1.0.dev4 — local P10-SD4 promotion candidate

- Promote the byte-bound SD3 repairs of exact supplied-coordinate Pareto geometry,
  finite scenario aggregation/ranking and strict economic-helper validation
  (F26/F27/F28).
- Integrate the unchanged 422-case pre-patch SD3 test file and the separately
  identified one-case post-patch CVaR refinement without rewriting chronology.
- Retain `exact=false` and `certified=false` for callback scenario results; no
  external objective, probability model, feasibility predicate or valuation is
  authenticated.
- Preserve the dev3 compositional-layer repair and all earlier historical records.
- No public release, licence, DOI, deposit, remote repository write or submission
  is authorised.

# 0.1.0.dev3 — local P10-SD2 promotion candidate

- Promote the hash-bound SD1 repair of the compositional-layer regret endpoint
  and strict representation checks (F24/F25). The rational stored-cut solvers
  and `verify-result` are unchanged.
- Retain the complete 280-case SD1 test file byte-for-byte and all original
  assertions except the three explicit version labels.
- Synchronise active software metadata and offline distribution helpers.
- No public release, licence, DOI, deposit or submission is authorised.

# 0.1.0.dev2 — local P10 repair candidate

- F22: verify public regret, proof/status/termination, all bound displays,
  incumbent absence and mirrored counters; disclose unverified metadata.
- Use one authoritative rational regret for its upward-rounded public display.
- Canonicalise infeasibility and callback absence in result producers.
- F23: validate cardinalities and boolean flags before conversion.
- Retain the P10 failing regressions and add boundary/alteration controls.
- Version-only amendments to three existing identity assertions are registered.

No public release, licence or identifier is implied. Earlier records follow.

# 0.1.0.dev1 — local P7 candidate

Stored-input exact weighted-cut route, strict arc support, projection checks, total-call B&B budgets, rational result records and independent finite semantic verification. Numeric callbacks and unvalidated dynamic/scenario outputs are explicitly uncertified. CLI output placement corrected. No public release. Full diffs and retained test revisions are in the phase package.

# Changelog

## 0.1.0.dev0 - 2026-09-12

- first materialised Generation-2 companion candidate;
- static PEP 621 metadata and `src/` package layout;
- deterministic public Python API and `cdrse` CLI;
- float64, high-precision audit and exact-rational backend separation;
- coordinate DD, weak-coupling cut and DSRG witness utilities;
- forest and bounded-treewidth resource dynamic programmes;
- exact/anytime branch and bound with concrete feasible incumbent semantics;
- dynamic min-cut, explicit-family and fixed-horizon trajectory solvers;
- machine-readable problem, result and certificate schemas;
- deterministic property-style, metamorphic, CLI, regression and clean-room tests;
- wheel and source-distribution build workflow;
- public-release licence, repository and DOI gates intentionally unresolved.
